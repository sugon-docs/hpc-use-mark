
# 基础使用

## 环境

```bash
export PATH=/your/path/bin:$PATH
export LD_LIBRARY_PATH=/your/path/lib:$LD_LIBRARY_PATH

export C_INCLUDE_PATH=/your/path/include:$C_INCLUDE_PATH # C语言 
export CPLUS_INCLUDE_PATH=/your/path/include:$CPLUS_INCLUDE_PATH # C++

```

## 后台运行

```bash
nohup command > "nohup_$(date +%Y%m%d_%H%M%S).out" 2>&1 & # 指定输出名字
```

## 追踪进程号 id位置

```bash
ll /proc/xxx

pwdx xxx
```

## mpi 

### 变量

```bash
export MPI_ROOT=$I_MPI_ROOT
```

## 文件权限

![[Pasted image 20250818172125.png]]

```bash
d代表的是目录(directroy) 
-代表的是文件(regular file) 
s代表的是套字文件(socket) 
p代表的管道文件(pipe)或命名管道文件(named pipe) 
l代表的是符号链接文件(symbolic link) 
b代表的是该文件是面向块的设备文件(block-oriented device file) 
c代表的是该文件是面向字符的设备文件(charcter-oriented device file)
```

```bash
-rw------- (600) 只有拥有者有读写权限。 
-rw-r--r-- (644) 只有拥有者有读写权限；而属组用户和其他用户只有读权限。 
-rwx------ (700) 只有拥有者有读、写、执行权限。 
-rwxr-xr-x (755) 拥有者有读、写、执行权限；而属组用户和其他用户只有读、执行权限。 
-rwx--x--x (711) 拥有者有读、写、执行权限；而属组用户和其他用户只有执行权限。 
-rw-rw-rw- (666) 所有用户都有文件读、写权限。 
-rwxrwxrwx (777) 所有用户都有读、写、执行权限。
```

## ulimit -xx unlimited

```bash
ulimit -f unlimited
ulimit -n unlimited
ulimit -s unlimited
ulimit -l unlimited
```
![[Pasted image 20250823195951.png]]

## 进程查看

```bash
lsof -i :8080   # 查看端口的进程 
ss -tulnp | grep :8080  # 查看端口的进程  

pwdx pid  # 打印出进程所在路径
```